export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <section className="w-full max-w-2xl rounded-lg border p-8">
        <h1 className="text-2xl font-semibold">TrustLine MVP</h1>
        <p className="mt-2 text-sm text-foreground/80">
          Base project initialized with Next.js App Router, TypeScript, and
          Tailwind CSS.
        </p>
      </section>
    </main>
  );
}
